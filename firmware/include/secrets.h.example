// 复制为 secrets.h 并填入真实值(secrets.h 已被 .gitignore 忽略)
#pragma once

#define WIFI_SSID     "你的WiFi名"
#define WIFI_PASSWORD "你的WiFi密码"

// MQTT broker 的局域网 IP —— 需与设备同一网段(本项目=飞牛 NAS)
#define MQTT_HOST "192.168.1.100"
#define MQTT_PORT 1883
#define MQTT_USER ""      // 匿名则留空
#define MQTT_PASS ""

// OTA 固件服务器(thumbserver,随 collector 所在机 = Mac Mini)。
// 与 broker 不同机时单独填;同机可省略(会回退用 MQTT_HOST)。
#define OTA_HOST  "192.168.1.101"
